import { Map } from 'immutable';
import * as C from '../util/Consts';
import { OverloadMap } from './Core';
export interface Definition {
    arity: number | number[];
    overloads: OverloadMap;
}
export declare const namedDefinitions: Map<C.NamedOperator, Definition>;
