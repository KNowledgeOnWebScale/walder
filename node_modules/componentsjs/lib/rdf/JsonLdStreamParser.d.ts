/// <reference types="node" />
import { Transform } from "stream";
/**
 * A JsonLdStreamParser takes a text stream as input and parses it to a triple stream.
 */
export declare class JsonLdStreamParser extends Transform {
    static BLANK_NODE_COUNTER: number;
    static DEFAULT_CONTEXTS: {
        [id: string]: string;
    };
    _blankNodeId: number;
    _data: string;
    _jsonld: any;
    _contexts: {
        [id: string]: string;
    };
    constructor(contexts?: {
        [id: string]: string;
    });
    _transform(chunk: any, encoding: any, done: any): void;
    _flush(done: any): void;
    _convertEntity(entity: any): any;
}
