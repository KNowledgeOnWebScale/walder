import { IJsonLdContextNormalized } from "jsonld-context-parser";
/**
 * A tree structure that holds all contexts,
 * based on their position in the JSON object.
 *
 * Positions are identified by a path of keys.
 */
export declare class ContextTree {
    private readonly subTrees;
    private context;
    getContext([head, ...tail]: string[]): Promise<IJsonLdContextNormalized>;
    setContext([head, ...tail]: string[], context: Promise<IJsonLdContextNormalized>): void;
}
