import * as RDF from "rdf-js";
import { BaseQuad } from "rdf-js";
/**
 * A wrapper around an RDF source that emits totalItems metadata *before* the end event.
 */
export declare class RdfSourceMetadata<Q extends BaseQuad = RDF.Quad> implements RDF.Source<Q> {
    private readonly source;
    constructor(source: RDF.Source);
    match(subject?: RDF.Term | RegExp, predicate?: RDF.Term | RegExp, object?: RDF.Term | RegExp, graph?: RDF.Term | RegExp): RDF.Stream<Q>;
}
