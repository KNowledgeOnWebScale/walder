'use strict';

/**
 * Handler interface.
 *
 * handlers are used for a specific task in the server workflow.
 *
 * @type {module.Handler}
 */
module.exports = class Handler {
  constructor() {
  }

  handle() {
  }
};