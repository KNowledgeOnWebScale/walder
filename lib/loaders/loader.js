'use strict';

/**
 * Loader interface.
 *
 * loaders are used to load data from external sources.
 *
 * @type {module.Loader}
 */
module.exports = class Loader {
  constructor() {
  }

  load() {
  }
};